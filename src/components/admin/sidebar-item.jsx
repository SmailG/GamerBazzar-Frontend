import React, { Component } from 'react';
import './style.scss';

const SidebarItem = ({ title, image, alt }) => {

    return (
        <div className="admin-sidebar_item">
            Games
        </div>
    );
}

export default SidebarItem;